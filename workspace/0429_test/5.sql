CREATE TABLE MEMBERS(
 ID VARCHAR2(20),
 AGE NUMBER
);



INSERT INTO MEMBERS (ID,AGE)						
VALUES ('sports',35);
INSERT INTO MEMBERS (ID,AGE)						
VALUES ('admin',40);
INSERT INTO MEMBERS (ID,AGE)						
VALUES ('brown',20);


SELECT *			
FROM MEMBERS;			


CREATE OR REPLACE PROCEDURE USER_PROC
AS
var_age NUMBER;
BEGIN
SELECT AGE INTO var_age FROM MEMBERS WHERE ID = 'admin';

IF var_age >= 40 THEN

 

var_age := var_age + 1;

 

END IF;

DBMS_OUTPUT.PUT_LINE (var_age);

END;

SET SERVEROUTPUT ON;
EXECUTE USER_PROC;